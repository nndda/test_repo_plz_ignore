name: Build Godot export templates

on:
  push:
    branches:
      - main
  workflow_dispatch:

defaults:
  run:
    shell: bash

jobs:
  build-desktop:
    env:
      GODOT_VERSION: 4.6.1-stable

    strategy:
      matrix:
        include:
          # Linux
          - target: linuxbsd
            arch: x86_64
            runner: ubuntu-22.04

          #- target: linuxbsd
          #  arch: x86_32
          #  runner: ubuntu-22.04

          # Windows
          - target: windows
            arch: x86_64
            runner: ubuntu-24.04

          #- target: windows
          #  arch: x86_32
          #  runner: ubuntu-24.04

    runs-on: ${{ matrix.runner }}

    steps:
      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.14.2"

      - name: Build platform ${{ matrix.target }}-${{ matrix.arch }}
        run: curl -fsSL https://raw.githubusercontent.com/nndda/build-godot/01afc04a7ac5bc54008735b5aa9a0f690dd8c426/build-exports.sh | bash
        env:
          EXPORT_PLATFORM: ${{ matrix.target }}
          EXPORT_ARCH: ${{ matrix.arch }}

      - name: Upload build artifact
        uses: actions/upload-artifact@v6
        with:
          name: export-template-${{ matrix.target }}-${{ matrix.arch }}
          path: |
            bin/*
            !bin/obj/
